// Write a program in C++ to compute the specified expressions and print the output.(25 * 3.5 - 3 * 7.5) + 3 / (40.5 - 4.5)

#include <iostream>
using namespace std;

int main()
{
    float expression = (25 * 3.5 - 3 * 7.5) + 3 / (40.5 - 4.5);
    cout << "The output of the expression is :" << expression;
    return 0;
}
